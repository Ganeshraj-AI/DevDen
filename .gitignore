# Environments
.env
.env.local
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Logs
logs/
*.log

# Caches
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
.hypothesis/
.mypy_cache/
.ruff_cache/

# Databases
# DO NOT IGNORE projects.json because PythonAnywhere needs the initial schema
# However, in production, we shouldn't overwrite the live projects.json with the local one
# So we will ignore it, but the user must upload it manually the first time or use a default.
# projects.json
# backups/

# IDEs
.idea/
.vscode/
*.swp
*.swo
